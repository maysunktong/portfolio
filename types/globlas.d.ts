type LinkButtonValues = {
  href: string;
  label: string;
};

type WorkType ={
  id: string,
  name:string,
  slug: string,
  stacks: string[],
  year: string,
  client: string,
  images: string[],
  video: string[]
}

type ParamsProp = {
  slug: string
}
